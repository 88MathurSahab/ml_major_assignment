# service.yaml
apiVersion: v1
kind: Service
metadata:
  name: olivetti-service
spec:
  type: NodePort 
  selector:
    app: olivetti-flask
  ports:
    - port: 5000 
      targetPort: 5000
      protocol: TCP